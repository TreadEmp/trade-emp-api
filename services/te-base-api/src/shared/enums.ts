export enum JOB_STATUS {
    PENDING = "pending",
    HIRED = "hired",
    COMPLETED = "completed",
}

export enum JOB_REQUEST_STATUS {
    ACTIVE = "active",
    INACTIVE = "inactive",
}

export enum BID_STATUS {
    PENDING = "pending",
    ACCEPTED = "accepted",
    REJECTED = "rejected",
}

export enum JOB_REQUEST_ACCEPTANCE_STATUS {
    PENDING = "pending",
    ONGOING = "ongoing",
    COMPLETED = "completed",
    CANCELED = "canceled",
}

export enum JOB_TYPES {
    JOB = "job",
    JOB_REQUEST = "job-request",
}
